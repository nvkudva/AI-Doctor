{
  "name": "virtual-doctor",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "bun --cwd apps/web dev",
    "build": "bun --cwd apps/web build",
    "typecheck": "bun --cwd apps/web typecheck"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
